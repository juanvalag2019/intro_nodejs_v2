/*
  - Node incluye un conjunto de modulos que son globales
    que se pueden importar y usar para diferentes tareas:
*/

// FileSystem (fs): modulo para interactuar con los archivos del sistema
import fs from 'node:fs';
console.log(fs);

// Path (path): modulo para manejar rutas de archivos y directorios dependiendo el ambiente


/* 
  child_process: modulo para crear y comunicar subprocesos en segundo plano,
                también permiten comunicar el proceso principal con los subprocesos
*/


/*
  http: modulo para comunicarse con http en la red donde se ejecuta Node,
  permite crear servidores http
*/
